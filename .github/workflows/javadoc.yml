name: Javadoc

on:
  push:
    tags:
      - "v[0-9]+.[0-9]+.[0-9]+"
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  deploy:
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}

    steps:
      - uses: actions/checkout@v7

      - name: Set up JDK 21
        uses: actions/setup-java@v5
        with:
          distribution: temurin
          java-version: 21
          cache: gradle

      - name: Grant execute permission for gradlew
        run: chmod +x gradlew

      - name: Generate Javadoc
        run: ./gradlew :kit:javadoc :excel-kit-spring:javadoc --no-daemon

      - name: Stage Javadoc site
        run: |
          mkdir -p build/javadoc-site
          cp -R kit/build/docs/javadoc/. build/javadoc-site/
          mkdir -p build/javadoc-site/excel-kit-spring
          cp -R excel-kit-spring/build/docs/javadoc/. build/javadoc-site/excel-kit-spring/

      - name: Setup Pages
        uses: actions/configure-pages@v6

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v5
        with:
          path: build/javadoc-site

      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v5
